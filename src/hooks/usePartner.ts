/** Re-export so existing imports of `@/hooks/usePartner` keep working. */
export { usePartner, usePartnerFeature } from '@/partner/PartnerContext';
export { PartnerProvider } from '@/partner/PartnerProvider';
